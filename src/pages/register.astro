---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Rejestracja - Discord Wannabe">
  <main class="min-h-screen flex items-center justify-center bg-background">
    <div id="register-page-root" class="w-full max-w-md p-6">
      <!-- React Island będzie zamontowany tutaj -->
    </div>
  </main>
</Layout>

<script>
  import { createRoot } from 'react-dom/client';
  import { RegisterPage } from '../components/auth/RegisterPage';
  import { createElement } from 'react';

  const container = document.getElementById('register-page-root');
  if (container) {
    const root = createRoot(container);
    root.render(createElement(RegisterPage));
  }
</script>
