import { rest } from "msw";

// Empty handlers file — tests can extend this with project-specific mocks.
export const handlers = [
  // Example:
  // rest.post("https://livekit.example/token", (req, res, ctx) => {
  //   return res(ctx.status(200), ctx.json({ token: "stub-token" }));
  // }),
];






